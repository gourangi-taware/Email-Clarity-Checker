#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../letter-count/bin/letter-count.js" "$@"
  ret=$?
else 
  node  "$basedir/../letter-count/bin/letter-count.js" "$@"
  ret=$?
fi
exit $ret
